name: 'Update CHANGELOG file'
description: 'Update the changelog file'
runs:
  using: 'docker'
  image: 'rcmachado/changelog'
  entrypoint: 'phpstan.sh'

branding:
  icon: 'check-square'
  color: 'purple'
